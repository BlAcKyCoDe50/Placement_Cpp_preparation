/*

Where last node points to first node
It does not contain null pointers
It has the biggest advantage of time saving when we want to go to first node rom from the last node
Directly points to first node

*/ 
#include<iostream>
using namespace std;

struct Node{
    int data;
    struct Node*next;
    struct Node*previous;
};

main(){
int n;
cin>>n;

return 0;
}