/*

Each node contains data and two pointers to point previous and next node
Main advantage is we can traverse in any direction, forward or backward
Drawback is it requires more memory compared to singly linked list beacause we ned an extra pointer

*/
